{/* const ingredients = [];

for (let i = 1; i <= 20; i += 1) {
  if (r[`strIngredient${i}`] !== "" && r[`strIngredient${i}`] != null) {
    ingredients.push(
      <li>
      <strong>{r[`strIngredient${i}`]}</strong>
      {r[`strMeasure${i}`]}
    </li>
    );
  }
}
console.log(ingredients);
for (const i in r) console.log(`${i}: ${r[i]}`) }*/}